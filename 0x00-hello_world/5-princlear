#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
	{
		char Anything[56] = "with proper grammar, but the outcome is a piece of art";
		prinf(Anything);

		return(0);
	}
